# swift
SWIFT (SHAP-Weighted Impact Feature Testing) — A model-aware distribution monitoring method for ML systems. Uses model decision boundaries for bucketing, SHAP values for normalization, and Wasserstein distance for comparison.
